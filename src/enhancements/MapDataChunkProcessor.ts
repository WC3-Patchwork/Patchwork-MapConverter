import { LoggerFactory } from "../logging/LoggerFactory";

const log = LoggerFactory.createLogger('MapDataChunkProcessor')

const MapDataChunkProcessor = {
    composeMapFiles: async function (input: DirectoryTree): Promise<>
}

export { MapDataChunkProcessor }